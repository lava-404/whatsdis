services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      WHATSDIS_ALLOWED_ORIGINS: "http://localhost:3000"
      WHATSDIS_MODEL_PATH: "yolov8n.pt"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 15s
      timeout: 5s
      retries: 5

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_API_URL: "http://localhost:8000"
    depends_on:
      - backend
