# URL of the Python detection service.
NEXT_PUBLIC_API_URL=http://localhost:8000

# Longest edge of the JPEG sent to the backend. One shot per analysis, so this
# can afford to be generous — bigger frames mean small objects survive.
NEXT_PUBLIC_CAPTURE_MAX_EDGE=960
